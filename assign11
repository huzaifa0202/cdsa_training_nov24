// Check if a number is 2 digit number

#include<stdio.h>
int main()
{
    int num;
    printf("enter the number:");
    scanf("%d",&num);
    if(num>=10 && num<=99)
    {
        printf(" the number is 2 digit number");
    }
}